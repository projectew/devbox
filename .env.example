# Copy only the names your application requires. Do not put live credentials in
# this repository, Docker build args, Dockerfile ENV instructions, or devcontainer.json.
EXAMPLE_API_BASE_URL=https://example.invalid

